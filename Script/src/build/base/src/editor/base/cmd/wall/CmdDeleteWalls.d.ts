import { CompositeCommand } from '../CompositeCommand';
export declare var WallTypeEnum: {
    free: string;
    notShared: string;
    shared: string;
};
export declare class CmdDeleteWalls extends CompositeCommand {
}
