import {Settings as BaseSettings} from '../../base/application/Settings';

//==========================================================
// <T>设置信息。</T>
//==========================================================
export class Settings extends BaseSettings {
   public htmlCanvas3d: HTMLCanvasElement;
}
