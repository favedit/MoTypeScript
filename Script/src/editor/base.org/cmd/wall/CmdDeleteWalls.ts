import {CompositeCommand} from '../CompositeCommand';

export var WallTypeEnum = {
    free: "free",
    notShared: "not_shared",
    shared: "shared"
};

export class CmdDeleteWalls extends CompositeCommand{

}
